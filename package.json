{
  "name": "symbiosis-stream-engine",
  "version": "1.0.0",
  "description": "Zero-buffer streaming proxy for cloud storage aggregation via Rclone RC",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
